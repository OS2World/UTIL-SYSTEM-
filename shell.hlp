The SHELL directive specifies the name of the file which contains the
command line processor used in the DOS Compatibility Environment (a.k.a
3.x Box).

Directive syntax:	SHELL = commandprocessorname [options]

Default value:		COMMAND.COM

Multiple occurrences:	No	

Affects protected mode:	No
			
Affects real mode:	Yes

Example:		SHELL = COMMAND.COM /P /E:1024

