The SWAPPATH directive controls the location of the swap file used by
OS/2's virtual memory manager.  The SWAPPATH directive is ignored if
segment swapping is disabled with the MEMMAN directive.

Directive syntax:	SWAPPATH = drive:[path]

Default value:		Root directory of boot drive

Multiple occurrences:	No	

Affects protected mode:	Yes
			
Affects real mode:	No

Example:		SWAPPATH = C:\TEMP

